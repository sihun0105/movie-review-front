import { FunctionComponent } from "react";
interface PageProps {}

const Page: FunctionComponent<PageProps> = ({}) => {
  return (
    <div>
      <h2>Login</h2>
    </div>
  );
};

export default Page;
