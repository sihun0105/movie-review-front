/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'file.koreafilm.or.kr',
      'uploads.drunkenmovie.shop',
      'image.tmdb.org',
    ],
  },
}

module.exports = nextConfig
